(function($) {
    $.extend(Tapestry.Initializer, {

        configurePopDetail: function(specs) {
           /* $('#' + specs.clientId).qtip(
                {
                    content: {
                        // Set the text to an image HTML string with the correct src URL to the loading image you want to use
                        text: specs.text,
                        ajax: {
                            url: specs.ajax // Use the rel attribute of each element for the url to load
                        },
                        title: {
                            text: false, // Give the tooltip a title using each elements text
                            button: false
                        }
                    },
                    position: {
                        at: 'right center', // Position the tooltip above the link
                        my: 'left center',
                        viewport: $(window), // Keep the tooltip on-screen at all times
                        effect: false // Disable positioning animation
                    },
                    show: {
                        event: 'mouseenter',
                        solo: true // Only show one tooltip at a time
                    },
                    hide: {
                        delay: 500,
                        fixed: true
                    },
                    style: {
                        classes: 'tip ui-tooltip-default ui-tooltip-wiki ui-tooltip-light ui-tooltip-rounded ui-tooltip-shadow ui-tooltip-pos-lc ' + specs.classes,
                        width: 440,
                        tip: {
                            border:1
                        }
                    }
                }).click(function(event) {  }); */
        }

    });
})(jQuery);
