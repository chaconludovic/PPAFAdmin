typeError = The file extension {file} is invalid. The following extensions are accepted: {extensions}
dropArea-label =Drop the files here to add them.
emptyError = {file} is empty, select a file.
minSizeError = {file} is too small, the minimum size is {minSizeLimit}.
onLeave =These files are being uploaded, if you quit now, the upload will be cancelled.
upload-label =Click here to select a document or drag it into this frame
cancel-label =Cancel
failed-label =Failure
sizeError = {file} is too large, the maximum permissible size is {sizeLimit}.
